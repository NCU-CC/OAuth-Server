<%= form_tag oauth_authorized_application_path(application), method: :delete do %>
  <%= button_tag t('doorkeeper.authorized_applications.buttons.revoke'), class: "waves-effect waves-light btn", onclick: "return confirm('#{ t('doorkeeper.authorized_applications.confirmations.revoke') }')", type: "submit" %>
<% end %>
